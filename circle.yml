machine:
  node:
    version: 6

dependencies:
  pre:
    - npm install -g yarn@0.16.1
  override:
    - yarn install

test:
  pre:
    - yarn run clean
    - yarn run pretest
  override:
    - yarn test

general:
  artifacts:
    - "./coverage/lcov-report/"
